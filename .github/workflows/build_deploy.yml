name: Build and push nuget

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

jobs:
  build-push-nuget:
    permissions:
      contents: read
      id-token: write  # enable GitHub OIDC token issuance for this job

    env:
      BUILD_CONFIG: "Release"
      SOLUTION: "IIIF.sln"
      DOTNET_VERSION: "8.0.x"

    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./src/IIIF

    steps:
      - name: Check out code
        id: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Setup GitVersion
        uses: gittools/actions/gitversion/setup@v4
        with:
          versionSpec: "6.x"

      - name: Determine GitVersion
        id: gitversion
        uses: gittools/actions/gitversion/execute@v4

      - name: Restore dependencies
        run: dotnet restore $SOLUTION

      - name: Build
        run: dotnet build $SOLUTION --configuration $BUILD_CONFIG --no-restore -p:ContinuousIntegrationBuild=true

      - name: Test
        run: dotnet test $SOLUTION /p:Configuration=$BUILD_CONFIG --no-restore --no-build --verbosity normal

      - name: Pack
        run: dotnet pack IIIF/IIIF.csproj --configuration $BUILD_CONFIG -p:Version=${{ steps.gitversion.outputs.semVer }} -p:PackageVersion=${{ steps.gitversion.outputs.semVer }} --no-restore --no-build

      - name: NuGet login (OIDC → temp API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Publish Package and Symbols
        if: startsWith(github.ref, 'refs/tags/v')
        run: dotnet nuget push '**/*.nupkg' --source 'https://api.nuget.org/v3/index.json' --api-key ${{steps.login.outputs.NUGET_API_KEY}}